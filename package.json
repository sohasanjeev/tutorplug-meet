{
  "name": "meet-platform",
  "version": "1.0.0",
  "private": true,
  "description": "TutorPlug - Full-stack tutoring video conferencing platform with permanent links, Gmail sign-in, and automatic continuous session recording",
  "scripts": {
    "install:server": "npm --prefix server install",
    "install:client": "npm --prefix client install",
    "install:all": "npm run install:server && npm run install:client",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build:server": "npm --prefix server run build",
    "build:client": "npm --prefix client run build",
    "build": "npm run build:server && npm run build:client"
  },
  "keywords": ["webrtc", "video-conferencing", "google-meet", "automatic-recording", "real-time"],
  "license": "ISC"
}
